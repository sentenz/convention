{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "rebaseWhen": "behind-base-branch",
  "dependencyDashboard": false,
  "prHourlyLimit": 0,
  "prConcurrentLimit": 0,
  "ignorePaths": [
    "**/node_modules/**",
    "**/bower_components/**",
    "**/charts/**"
  ],
  "labels": [
    "dependencies"
  ],
  "vulnerabilityAlerts": {
    "labels": [
      "security"
    ],
    "automerge": false
  },
  "customManagers": [
    {
      "customType": "regex",
      "description": "Safely update container images in Makefile variables (handles tag-only or tag@sha256)",
      "managerFilePatterns": [
        "**/[Mm]akefile"
      ],
      "matchStrings": [
        "(?<depName>(?:(?:docker\\.io|ghcr\\.io|quay\\.io|gcr\\.io|mcr\\.microsoft\\.com|registry\\.gitlab\\.com|cgr\\.dev|oci\\.io)/)?[A-Za-z0-9_.-]+(?:/[A-Za-z0-9_.-]+)+):(?<currentValue>[^@\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]{64}))?"
      ],
      "autoReplaceStringTemplate": "{{depName}}:{{newValue}}{{#if newDigest}}@{{newDigest}}{{/if}}",
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Introduces breaking changes or require code adjustments due to changes in public APIs.",
      "matchUpdateTypes": [
        "major"
      ],
      "groupName": "major versions",
      "groupSlug": "major-versions",
      "automerge": false
    },
    {
      "description": "Introduces backward-compatible functionality improvements and feature additions.",
      "matchUpdateTypes": [
        "minor"
      ],
      "groupName": "minor versions",
      "groupSlug": "minor-versions",
      "automerge": false
    },
    {
      "description": "Introduces backward-compatible bug fixes and small corrections.",
      "matchUpdateTypes": [
        "patch"
      ],
      "groupName": "patch versions",
      "groupSlug": "patch-versions",
      "automerge": false
    },
    {
      "description": "Docker base image digest bumps across Dockerfiles, Containerfiles, and Makefiles.",
      "matchManagers": [
        "dockerfile",
        "containerfile",
        "regex"
      ],
      "matchDatasources": [
        "docker"
      ],
      "matchUpdateTypes": [
        "digest"
      ],
      "groupName": "docker digest updates",
      "groupSlug": "docker-digests",
      "automerge": false
    }
  ]
}
